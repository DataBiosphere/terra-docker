# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
# python3 is the name of the conda environment, must match environment name in environment.yml
__conda_setup="$('/home/jupyter/.envs/python3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
eval "$__conda_setup"
else
if [ -f "/home/jupyter/.envs/python3/etc/profile.d/conda.sh" ]; then
. "/home/jupyter/.envs/python3/etc/profile.d/conda.sh"
else
export PATH="/home/jupyter/.envs/python3/bin:$PATH"
fi
fi
unset __conda_setup
# <<< conda initialize <<<